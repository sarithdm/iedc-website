{
  "name": "iedc-website-railway",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install --production"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
